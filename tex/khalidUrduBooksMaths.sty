\ProvidesPackage{khalidUrduBooksMaths}
\usepackage{fontspec}
\usepackage{makeidx}
\usepackage{amsmath}                                                                         %\tfrac for fractions in text
\usepackage{amssymb}      
\usepackage{amsthm}      						%theorem environment. started using in the maths book
\usepackage{mathtools}
\usepackage{commath}									%differentiation symbols
\usepackage{bm}											%bold greek letters too
\usepackage{lineno}										%line numbers in pdf
\usepackage{graphics}
\usepackage[]{graphicx} 
\usepackage{cancel}
\usepackage{mathpazo}              %kkk   causes trouble with siunitx units such as ohm which needs correction using sisetup
\usepackage{booktabs}                                  %for tables toprule, midrule and bottomrule
\usepackage{parskip} 
\usepackage[stable,bottom]{footmisc}						%for footnotes in the headings

\usepackage{siunitx} 			%SI units that meets iso-31 regulations such as \micro, \degree \SI{3}{\celsius}
%\sisetup{per=frac,fraction=nice}    %setup for siunitx
\sisetup{per=reciprocal,fraction=nice}    %setup for siunitx

\protected\def\numpi{\text{\ensuremath{\pi}}}  %25 june 2015, these two lines were added to be able to use numpi as pi in SI commands.
\sisetup{input-symbols = \numpi}					%without it, in the presence of mathpazo package, SI would show beta instead of pi
\DeclareSIUnit\litre{\liter}
\DeclareSIUnit{\pH}{pH}

\usepackage{steinmetz}		%\phase to show angle of phasors
\usepackage[version=3]{mhchem}     %chemical symbols such as \ce{H2O}

\usepackage[]{geometry} %use this command and put b5paper in documentclass statement to set b5papersize
%\usepackage[paperwidth=8in,paperheight=10.25in,]{geometry}  %to use b5paper size, use the command above for using geometry package
%\usepackage[margin=1cm]{geometry}                        %the geometry package gives correct page size in pdf meta data i.e. pdf file properties
%\pdfpagewidth=\paperwidth              %instead of the geometry package, could have used these two lines      
%\pdfpageheight=\paperheight

% \usepackage{microtype}               %underfull messages try using it if needed. check first if it works with xelatex

\usepackage{makeidx}                %  \makeindex 
\usepackage{placeins}                   % \FloatBarrier
\usepackage{color}
\usepackage{caption}                               %must be placed before bidi
\usepackage{subcaption}

%\usepackage{hyperref}                   %loaded second last just before polyglossia. still giving problems
\usepackage{polyglossia}                %is loaded the last


%\renewcommand\thelinenumber{\textenglish{\arabic{linenumber}}}
%\linenumbers												%for line numbers in pdf

\setmainlanguage[numerals=maghrib]{arabic}     %for english numbers use numerals=maghrib, for arabic numerals=arabicdigits
\setotherlanguages{english}

\newfontfamily\arabicfont[Scale=1.0,Script=Arabic]{Jameel Noori Nastaleeq} 
\setmonofont{DejaVu Sans Mono}                                                                  %had to add this and the next line to get going after ubuntu upgrade
\let\arabicfontt\ttfamily                                                                                  %had to add this and the above line to get going after ubuntu upgrade
\newfontfamily\urduTechTermsfont[Scale=1.0,Script=Arabic]{AA Sameer Sagar Nastaleeq Bold}
\newfontfamily\urdufont[WordSpace=60.0,Script=Arabic]{Jameel Noori Nastaleeq}
\newfontfamily\urdufontBig[Scale=1.25,WordSpace=60.0,Script=Arabic]{Jameel Noori Nastaleeq}
\newfontfamily\urdufontItalic[Scale=1.25,WordSpace=60.0,Script=Arabic]{Jameel Noori Nastaleeq Italic}
\setlength{\parskip}{5mm plus 4mm minus 3mm}


%%\newfontfamily\AcaciasHand{AcaciasHand}

\setcounter{secnumdepth}{3} 
%=====================================
%included to maths sty but gives a english dot (.) after (theorem etc) names, so not using it for now. i also had trouble with the slanting fonts used, since i only have plain noori nastaleeq only. i was able to solve this by putting the command \theoremstyle{plain} before every theorem (cor, lemm etc) evvironment 
\newtheorem{theorem}{مسئلہ}[chapter]
\newtheorem{corollary}{ضمنی نتیجہ}[theorem]
\newtheorem{lemma}[theorem]{مسلمہ قطیہ}
\newtheorem*{remark}{قابل توجہ}
\newtheorem{definition}{تعریف}[chapter]
%\renewcommand\qedsymbol{$\blacksquare$} %to change the QED symbol at the end of proofs
\theoremstyle{remark}
\theoremstyle{definition}

